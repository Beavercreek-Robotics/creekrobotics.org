{
  "name": "creekrobotics.org",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:events": "node ./scripts/generate-events-json.mjs",
    "predev": "npm run generate:events",
    "prebuild": "npm run generate:events",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "vue": "^3.5.32",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.5",
    "vite": "^8.0.4"
  }
}
